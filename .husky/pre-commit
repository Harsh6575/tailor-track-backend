#!/usr/bin/env sh
set -e  # stop on error

GREEN="\033[0;32m"
RED="\033[0;31m"
YELLOW="\033[1;33m"
NC="\033[0m" # No Color

echo "${YELLOW}🧹 Running lint, format check, and type-check before commit...${NC}"
pnpm lint
pnpm format:check
pnpm type-check
echo "${GREEN}✅ All checks passed!${NC}"

echo "${YELLOW}🧪 Running tests...${NC}"
pnpm test
echo "${GREEN}✅ All tests passed!${NC}"
